#include <iostream>

#include "tests/TestsList.h"
#include "tests/TestsToken.h"

int main() {
  /* =================== TESTS =================== */
  /* =================== Lists =================== */
  testIntList();
  testStringList();
  /* ============================================= */
  /* ============================================= */

  /* =================== TESTS =================== */
  /* =================== Token =================== */
  testToken();
  /* ============================================= */
  /* ============================================= */

  return 0;
}